# Google OAuth — Web client ID from Google Cloud Console
# Used by @react-native-google-signin/google-signin to request the Firebase ID token.
# Firebase/iOS/Android configuration comes from GoogleService-Info.plist and google-services.json.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
